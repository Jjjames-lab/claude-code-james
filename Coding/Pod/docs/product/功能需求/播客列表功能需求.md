# 播客列表功能需求文档

**创建日期**: 2026-01-29
**产品名称**: Bookshelf Sounds
**产品愿景**: 我们不追求去抢夺任何人的注意力，而更愿意保护你的注意力，能放到你感兴趣的地方。这里是一个可以帮助你更好使用播客的地方，帮助你记录你的笔记、理解播客内容、发散你的好奇心。这里是像星际穿越中的男主女儿会在的书柜旁，很安心，虽然没有人在旁边，但是书柜里其实是有个爱你的人在陪伴着你啊。

---

## 一、功能概述

支持两种输入链接类型，提供不同的展示和交互方式：
1. **单集节目链接** (`/episode/xxx`) - 同时显示 Podcast 和 Episode 两个区域
2. **播客主页链接** (`/podcast/xxx`) - 先显示 Podcast 信息区，点击后展开节目清单

---

## 二、用户流程设计

### 场景 1：用户输入单集节目链接 (`/episode/xxx`)

```
步骤 1: 用户输入单集链接
  https://www.xiaoyuzhoufm.com/episode/xxx
    ↓
步骤 2: 同时显示两个可点击区域（上下布局）

参考图位置：/Users/tbingy/Desktop/Claude Code/Coding/Pod/docs/product/参考图/场景 1：用户输入单集节目链接跳转后界面.png
  ┌─────────────────────────────────────┐
  │ 📻 Podcast 信息区（可点击）         │ ← 点击 → 显示跳转新的页面，展示播客详情 + 最新 5 期清单
  │ - 播客名称                            │
  │ - 主播名称                            │
  └─────────────────────────────────────┘
                ↓
  ┌─────────────────────────────────────┐
  │ 🎙️ Episode 列表区（可点击）         │ ← 点击 → 进入 Tab 功能区
  │                                    │    (Overview/TRANSCRIPT/CHAPTERS/NOTE)
  │ - 单集标题                          │
  │ - 播客名称                          │
  └─────────────────────────────────────┘

步骤 3: 用户选择点击
  ├─ 点击 Podcast 信息区 → 显示播客详情 + 最新 5 期清单（同场景2的展开状态）
  └─ 点击 Episode 列表区 → 进入 Tab 功能区
```

#### 关键交互细节：
- **Podcast 信息区样式**：
  - 简洁卡片，只显示播客名称和主播名出
  - Hover 效果：边框高亮、轻微放大（scale 1.01）

- **Episode 列表区样式**：
  - 单集 标题 + 播客名称 

---

### 场景 2：用户输入播客主页链接 (`/podcast/xxx`)

```
步骤 1: 用户输入播客主页链接
  https://www.xiaoyuzhoufm.com/podcast/xxx
    ↓
步骤 2: 只显示 Podcast 信息区（可点击）

   ┌─────────────────────────────────────┐
  │ 📻 Podcast 信息区（可点击）         │ ← 点击 → 跳转新的页面显示，展示播客详情 + 最新 5 - 播客名称                          │                              期清单
  │   - 主播名称                                                                
  └─────────────────────────────────────┘

步骤 3: 用户点击 Podcast 信息区
    ↓
步骤 4: 跳转新的页面，单独展示播客节目信息

  ┌─────────────────────────────────────┐
  │               播客名                 
  │   [播客logo]   主播名                 │
  │               播客介绍                │
  │   ─────────────────────────────     │
  │   ┌─────────────────────────────┐  │
  │   │ [封面] 节目标题             │  │ ← 可点击
  │   │        时长 · 发布日期       │  │
  │   └─────────────────────────────┘  │
  │   ┌─────────────────────────────┐  │
  │   │ [封面] 节目标题             │  │ ← 可点击
  │   │        时长 · 发布日期       │  │
  │   └─────────────────────────────┘  │
  │   ... (共5期)                     │
  └─────────────────────────────────────┘

步骤 5: 用户点击某一集
    ↓
步骤 6: 直接进入 Tab 功能区
  - Overview（概览）
  - TRANSCRIPT（逐字稿）
  - CHAPTERS（章节）
  - NOTE（笔记）
```

#### 关键交互细节：

## 三、智能数据复用逻辑（重要！）

### 核心问题：避免重复转录已处理的单集

#### 数据来源判断（Q1-Q2）
```
Q1: 如何判断该单集是否已处理？
    ↓
检查 LocalStorage 中是否存在该 episodeId 的完整数据：
  - transcript（逐字稿）
  - chapters（章节）
  - notes（笔记）
    ↓
存在 → 标记为"已处理"
不存在 → 标记为"未处理"

Q2: 数据从哪里获取？
    ↓
已处理：从 LocalStorage 读取
未处理：调用后端 API 转录
```

#### 播客列表页面显示逻辑（Q3）
```
在场景2的节目清单中，每集显示状态：

┌─────────────────────────────────────┐
│ [封面] 节目标题                     │
│        时长 · 发布日期              │
│                                    │
│ [✓ 已处理] 或 [Run AI Processing]  │ ← 状态标识
└─────────────────────────────────────┘

已处理：
  - 显示绿色 ✓ 标记
  - 显示 "已处理" 文字
  - 点击 → 直接加载 LocalStorage 数据 → 进入 Tab 功能区

未处理：
  - 进入 Tab 功能区同步悬浮显示 "Run AI Processing" 按钮（主要按钮样式）→ 等待用户点击按钮，点击后调用转录 API 和AI章节功能并显示进度 → 完成后去掉提示，用户可以直接使用tab功能区
  - 
```

#### 点击某一集后的流程（Q4）
```
用户点击某一集
    ↓
检查该集处理状态
    ↓
┌─────────────┬──────────────┐
│  已处理      │   未处理      │
└─────────────┴──────────────┘
      ↓              ↓
从 LocalStorage  进入 Tab 功能区，悬浮显示 "Run AI Processing" 按钮（主要按钮样式）
读取数据          ↓
      ↓         等待用户点击按钮，点击后调用转录 API 和AI章节功能并显示进度
直接进入         ↓
Tab 功能区    完成后去掉提示，用户可以直接使用tab功能区
               ↓
            保存到 LocalStorage
```

---

## 四、Tab 功能区重构（延后实现）

### 当前 Tab 结构
```
概览 | 逐字稿 | 章节 | 节目单
```

### 新 Tab 结构
```
Overview | TRANSCRIPT | CHAPTERS | NOTE
```

### 变更说明
| 原名称 | 新名称 | 说明 |
|--------|--------|------|
| 概览 | Overview | 英文化，更简洁 |
| 逐字稿 | TRANSCRIPT | 英文化，统一风格 |
| 章节 | CHAPTERS | 英文化，统一风格 |
| - | NOTE | **新增**笔记功能 |
| 节目单 | **移除** | 不再需要（播客列表页面已提供） |

**优先级**: 低（先完成播客列表功能）

---

## 五、UI/UX 设计规范

### 颜色系统
```css
--primary-accent: rgba(212, 197, 185, 0.7);  /* 主色调：米色 */
--bg-card: rgba(255, 255, 255, 0.03);       /* 卡片背景 */
--bg-card-hover: rgba(255, 255, 255, 0.06);  /* 卡片 Hover 背景 */
--border-default: rgba(255, 255, 255, 0.08); /* 默认边框 */
--border-highlight: rgba(212, 197, 185, 0.2); /* 高亮边框 */
```

### 动画时长
```css
--transition-fast: 150ms;      /* 快速交互 */
--transition-normal: 250ms;    /* 常规交互 */
--transition-slow: 300ms;      /* 缓慢动画 */
```

### 缓动函数
```css
--easing-smooth: cubic-bezier(0.25, 0.46, 0.45, 0.94);
```

### 字体大小
```css
--text-xs: 0.75rem;     /* 12px - 辅助信息 */
--text-sm: 0.875rem;    /* 14px - 次要信息 */
--text-base: 1rem;      /* 16px - 正文 */
--text-lg: 1.125rem;    /* 18px - 小标题 */
--text-xl: 1.5rem;      /* 24px - 大标题 */
--text-3xl: 2rem;       /* 32px - 播客名称 */
```

---

## 六、技术实现要点

### 前端组件设计

#### 1. PodcastInfoCard 组件
```tsx
interface PodcastInfoCardProps {
  // 简洁模式（场景1）
  mode: 'simple' | 'full';

  // 播客信息
  podcastName: string;
  podcastDescription?: string;
  logo?: string;
  hostName?: string;
  totalEpisodes?: number;

  // 点击事件
  onClick: () => void;

  // 是否可展开（场景2）
  expandable?: boolean;
  expanded?: boolean;
  onToggle?: () => void;

  // 节目清单（展开时显示）
  episodes?: EpisodeInfo[];
}
```

#### 2. EpisodeListItem 组件
```tsx
interface EpisodeListItemProps {
  episodeId: string;
  episodeTitle: string;
  coverImage?: string;
  duration: number;
  createdAt: string;

  // 处理状态
  processed: boolean;  // 是否已处理

  // 点击事件
  onClick: () => void;

  // 样式变体
  variant?: 'card' | 'list';  // card = 大卡片, list = 列表项
}
```

#### 3. 状态管理
```tsx
// 新增应用状态
type AppState =
  | 'input'           // 输入链接
  | 'parsed'          // 单集解析完成（场景1）
  | 'podcast-info'    // 播客信息页（场景2初始状态）
  | 'podcast-expanded' // 播客展开状态（场景2展开后）
  | 'transcribing'    // 转录中
  | 'completed'       // 转录完成
  | 'history'         // 历史记录
  | 'settings';       // 设置
```

### LocalStorage 数据结构

#### 已处理单集标记
```typescript
interface ProcessedEpisode {
  episodeId: string;
  podcastId: string;
  episodeTitle: string;
  podcastName: string;
  processedAt: string;  // ISO 8601 时间戳
}

// 存储键：`processed_episodes`
// 结构：Map<episodeId, ProcessedEpisode>
```

#### 完整单集数据
```typescript
interface EpisodeData {
  episodeId: string;
  podcastId: string;
  episodeTitle: string;
  podcastName: string;
  coverImage: string;
  audioUrl: string;
  duration: number;
  showNotes: string;

  // 转录数据
  transcript?: TranscriptWord[];
  chapters?: Chapter[];
  notes?: Note[];

  // 元数据
  processedAt: string;
}
```

### API 设计

#### 播客主页解析 API
```typescript
POST /api/crawler/parse-podcast

Request:
{
  "url": "https://www.xiaoyuzhoufm.com/podcast/xxx"
}

Response:
{
  "success": true,
  "data": {
    "podcast_id": "xxx",
    "podcast_name": "播客名称",
    "host_name": "主播名称",
    "description": "播客介绍",
    "logo": "https://...",
    "episodes": [
      {
        "episode_id": "xxx",
        "episode_title": "节目标题",
        "audio_url": "https://...",
        "duration": 3600,
        "cover_image": "https://...",
        "show_notes": "...",
        "created_at": "2026-01-29T10:00:00Z"
      }
    ],
    "total_episodes": 100
  }
}
```

---

## 七、开发优先级

### Phase 1：核心功能（当前）
- [x] 后端播客主页解析 API
- [ ] 场景1：单集链接双区域显示
- [ ] 场景2：播客主页展开/收起
- [ ] 智能数据复用逻辑
- [ ] LocalStorage 状态管理

### Phase 2：UI/UX 优化
- [ ] 平滑动画优化
- [ ] 加载状态优化
- [ ] 错误处理优化
- [ ] 响应式适配

### Phase 3：Tab 重构（延后）
- [ ] Tab 重命名（英文化）
- [ ] 新增 NOTE tab
- [ ] 移除节目单 tab

---

## 八、验收标准

### 功能完整性
- ✅ 支持单集链接解析
- ✅ 支持播客主页链接解析
- ✅ 场景1：双区域正确显示
- ✅ 场景2：展开/收起流畅
- ✅ 智能数据复用不重复转录
- ✅ LocalStorage 数据正确读写

### 交互体验
- ✅ 点击响应迅速（< 100ms）
- ✅ 动画流畅（60fps）
- ✅ Hover 效果自然
- ✅ 状态提示清晰

### 边界情况
- ✅ 网络错误处理
- ✅ 解析失败处理
- ✅ LocalStorage 容量限制
- ✅ 大量节目性能（100+ 集）

---

## 九、备注

- 所有组件遵循现有设计系统（v3.0）
- 所有文案保持温暖、克制的语调
- 遵循"慢下来，深思考"的产品愿景
- 优先考虑用户体验，而非功能堆砌

---

**最后更新**: 2026-01-29
**状态**: 待实现
**优先级**: P0（核心功能）
